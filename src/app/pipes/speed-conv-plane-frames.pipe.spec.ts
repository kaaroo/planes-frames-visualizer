import { SpeedConversionPlaneFramesPipe } from './speed-conv-plane-frames.pipe';

describe('SpeedUnitConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeedConversionPlaneFramesPipe();
    expect(pipe).toBeTruthy();
  });
});
