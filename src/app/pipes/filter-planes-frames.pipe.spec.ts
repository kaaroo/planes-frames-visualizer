import { FilterPlanesFramesPipe } from './filter-planes-frames.pipe';

describe('FilterPlanesFramesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlanesFramesPipe();
    expect(pipe).toBeTruthy();
  });
});
